query GetSpells {
  spells {
    desc
    school {
      name
    }
    name
    damage {
      damage_at_character_level {
        damage
        level
      }
    }
    material
    components
    casting_time
  }
}
